/**
 * LICENSE_PLACEHOLDER
 **/

/**
 * Form entity description
 */
const Criteria = React.PropTypes.shape({
  type: React.PropTypes.string.isRequired,
  container: React.PropTypes.string.isRequired,
})

export default Criteria
